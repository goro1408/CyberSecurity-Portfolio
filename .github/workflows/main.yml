name: CI

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r projects/network-scanner/requirements.txt

    - name: Run Network Scanner Tests
      run: |
        # Here you would run any tests or linters for your project.
        # Assuming you have tests, you can run them like this:
        # pytest projects/network-scanner/tests/
        echo "No tests defined yet."
